-- Services
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")

-- Protect GUI
local ParentTarget = (gethui and gethui()) or CoreGui or Players.LocalPlayer:WaitForChild("PlayerGui")

-- Clean old instance
if ParentTarget:FindFirstChild("ModernSelectGui") then
    ParentTarget:FindFirstChild("ModernSelectGui"):Destroy()
end

-- Create ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ModernSelectGui"
ScreenGui.Parent = ParentTarget
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Frame (Background Window)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.Size = UDim2.new(0, 0, 0, 0) -- For Entrance Animation
MainFrame.ClipsDescendants = true
MainFrame.Active = true
MainFrame.Draggable = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 16)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 1.5
MainStroke.Color = Color3.fromRGB(60, 60, 80)
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
MainStroke.Parent = MainFrame

-- Main Gradient
local MainGradient = Instance.new("UIGradient")
MainGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0.00, Color3.fromRGB(24, 25, 36)),
    ColorSequenceKeypoint.new(1.00, Color3.fromRGB(14, 14, 20))
}
MainGradient.Rotation = 45
MainGradient.Parent = MainFrame

-- Top Bar Title
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 20, 0, 15)
Title.Size = UDim2.new(1, -40, 0, 25)
Title.Font = Enum.Font.GothamBold
Title.Text = "SELECT SCRIPT"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 2

-- Subtitle / Status
local SubTitle = Instance.new("TextLabel")
SubTitle.Name = "SubTitle"
SubTitle.Parent = MainFrame
SubTitle.BackgroundTransparency = 1
SubTitle.Position = UDim2.new(0, 20, 0, 36)
SubTitle.Size = UDim2.new(1, -40, 0, 15)
SubTitle.Font = Enum.Font.Gotham
SubTitle.Text = "Choose your execution mode"
SubTitle.TextColor3 = Color3.fromRGB(140, 140, 160)
SubTitle.TextSize = 12
SubTitle.TextXAlignment = Enum.TextXAlignment.Left
SubTitle.ZIndex = 2

-- Function: Setup Button Styling & Animations
local function StyleButton(btn, primaryColor, hoverColor)
    btn.AutoButtonColor = false
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = btn

    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 1
    stroke.Color = primaryColor
    stroke.Transparency = 0.6
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Parent = btn

    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0.00, primaryColor),
        ColorSequenceKeypoint.new(1.00, hoverColor)
    }
    gradient.Rotation = 90
    gradient.Parent = btn

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {Size = btn.Size + UDim2.new(0, 0, 0, 2)}):Play()
        TweenService:Create(stroke, TweenInfo.new(0.2), {Transparency = 0}):Play()
    end)

    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {Size = btn.Size - UDim2.new(0, 0, 0, 2)}):Play()
        TweenService:Create(stroke, TweenInfo.new(0.2), {Transparency = 0.6}):Play()
    end)
end

-- Farm Only Button
local FarmOnlyBtn = Instance.new("TextButton")
FarmOnlyBtn.Name = "FarmOnlyBtn"
FarmOnlyBtn.Parent = MainFrame
FarmOnlyBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
FarmOnlyBtn.Position = UDim2.new(0.08, 0, 0.38, 0)
FarmOnlyBtn.Size = UDim2.new(0.84, 0, 0, 42)
FarmOnlyBtn.Font = Enum.Font.GothamBold
FarmOnlyBtn.Text = "Farm Only"
FarmOnlyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmOnlyBtn.TextSize = 14
FarmOnlyBtn.TextStrokeTransparency = 0.5
FarmOnlyBtn.ZIndex = 2
StyleButton(FarmOnlyBtn, Color3.fromRGB(0, 140, 255), Color3.fromRGB(0, 90, 200))

-- Farm and Combat Button
local FarmCombatBtn = Instance.new("TextButton")
FarmCombatBtn.Name = "FarmCombatBtn"
FarmCombatBtn.Parent = MainFrame
FarmCombatBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
FarmCombatBtn.Position = UDim2.new(0.08, 0, 0.66, 0)
FarmCombatBtn.Size = UDim2.new(0.84, 0, 0, 42)
FarmCombatBtn.Font = Enum.Font.GothamBold
FarmCombatBtn.Text = "⚔️   Farm and Combat"
FarmCombatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmCombatBtn.TextSize = 14
FarmCombatBtn.TextStrokeTransparency = 0.5
FarmCombatBtn.ZIndex = 2
StyleButton(FarmCombatBtn, Color3.fromRGB(235, 60, 80), Color3.fromRGB(180, 40, 60))

-- Smooth Close Function
local function DestroyGui()
    local closeTween = TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0)
    })
    closeTween:Play()
    closeTween.Completed:Connect(function()
        ScreenGui:Destroy()
    end)
end

-- Events
FarmOnlyBtn.MouseButton1Click:Connect(function()
    DestroyGui()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xqmt/San-Diego/refs/heads/main/Test%20v3"))()
end)

FarmCombatBtn.MouseButton1Click:Connect(function()
    DestroyGui()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xqmt/San-Diego/refs/heads/main/Tooook"))()
end)

-- Entrance Animation (Fade-in Pop Up)
TweenService:Create(MainFrame, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 320, 0, 210)
}):Play()
